# DemoLab


## personal notes:
launch venv using `.\venv\Scripts\Activate`


## Order of tasks:
Fix the issue where the script could fail if there is too much usage. Might need to throttle somehow if usage reaches a limit.
